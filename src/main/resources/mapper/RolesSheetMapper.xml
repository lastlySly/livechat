<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lastlysly.mapper.RolesSheetMapper">
  <resultMap id="BaseResultMap" type="cn.lastlysly.pojo.RolesSheet">
    <result column="roles_id" jdbcType="INTEGER" property="rolesId" />
    <result column="roles_username" jdbcType="VARCHAR" property="rolesUsername" />
    <result column="roles_name" jdbcType="VARCHAR" property="rolesName" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    roles_id, roles_username, roles_name
  </sql>
  <select id="selectByExample" parameterType="cn.lastlysly.pojo.RolesSheetExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from roles_sheet
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <delete id="deleteByExample" parameterType="cn.lastlysly.pojo.RolesSheetExample">
    delete from roles_sheet
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="cn.lastlysly.pojo.RolesSheet">
    insert into roles_sheet (roles_id, roles_username, roles_name
      )
    values (#{rolesId,jdbcType=INTEGER}, #{rolesUsername,jdbcType=VARCHAR}, #{rolesName,jdbcType=VARCHAR}
      )
  </insert>
  <insert id="insertSelective" parameterType="cn.lastlysly.pojo.RolesSheet">
    insert into roles_sheet
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="rolesId != null">
        roles_id,
      </if>
      <if test="rolesUsername != null">
        roles_username,
      </if>
      <if test="rolesName != null">
        roles_name,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="rolesId != null">
        #{rolesId,jdbcType=INTEGER},
      </if>
      <if test="rolesUsername != null">
        #{rolesUsername,jdbcType=VARCHAR},
      </if>
      <if test="rolesName != null">
        #{rolesName,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="cn.lastlysly.pojo.RolesSheetExample" resultType="java.lang.Long">
    select count(*) from roles_sheet
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map">
    update roles_sheet
    <set>
      <if test="record.rolesId != null">
        roles_id = #{record.rolesId,jdbcType=INTEGER},
      </if>
      <if test="record.rolesUsername != null">
        roles_username = #{record.rolesUsername,jdbcType=VARCHAR},
      </if>
      <if test="record.rolesName != null">
        roles_name = #{record.rolesName,jdbcType=VARCHAR},
      </if>
    </set>
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map">
    update roles_sheet
    set roles_id = #{record.rolesId,jdbcType=INTEGER},
      roles_username = #{record.rolesUsername,jdbcType=VARCHAR},
      roles_name = #{record.rolesName,jdbcType=VARCHAR}
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
</mapper>